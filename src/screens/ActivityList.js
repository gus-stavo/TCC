import React from 'react'
import { View, Text } from 'react-native'

export default class ActivityList extends React.Component {
    render() {
        return (
            <Text>Lista deAtividades</Text>
        )
    }
}